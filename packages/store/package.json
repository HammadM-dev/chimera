{
  "name": "@chimera/store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SQLite persistence, migrations, and the credential vault.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@chimera/core": "*",
    "better-sqlite3": "^13.0.3",
    "@napi-rs/keyring": "^1.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0"
  }
}
