## What this changes

<!-- The reasoning, not the diff. What was wrong, and why this is the fix. -->

## How it was verified

<!--
Which checks were run, and — for a bug fix — how you know the test would have
failed before. A test that passes against the broken code is worse than none.
-->

- [ ] `npm run format`
- [ ] `npm run lint`
- [ ] `npm run typecheck`
- [ ] `npm test`
- [ ] `npm run test:e2e --workspace @chimera/desktop` <!-- if the UI or a run path changed -->

## Anything that needed a decision

<!--
A guard weakened, a rule narrowed, a schema changed. If the workflow document
shape moved, `schemaVersion` and `docs/WORKFLOW_SCHEMA.md` belong in this same
commit. Say "nothing" if nothing.
-->
